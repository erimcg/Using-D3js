- None of the links in any of the layouts are used correctly, currently lines are being drawn when paths with a .attr("d", d3.linkDirection()) could be used.

- When popping out a new window, the code that is copied over is the original code, not any potential modified code the user adds in

- When using a popped out window, if the page that the window was popped out from is reloaded or changed, the popup window ceases to function in any capacity

- We dont cover the new selection.join() in d3.v5

- Opinion on having the Svgs centered instead of left orientated